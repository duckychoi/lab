---
title: Raw — 인제스트 대기열
updated: 2026-09-07 (대기 0건 — 09-07 자동수집 13건 전량 처리 완료)
---

# Raw 대기열

인제스트할 소스를 여기에 추가한다.
LLM이 처리(ingest) 완료하면 해당 항목을 즉시 삭제한다.

형식:
```
## 소스 제목
- type: url | text | file
- url: https://...
- 메모: (선택사항)
```

---

## 대기 중: **0건**

2026-09-07 자동수집 **13건 전량 처리 완료**(2026-09-07) — GitHub 5 · HF논문 5 · HF모델 3.
**중복 0건**(09-06 도입 URL 대조 필터 적용, 09-06 배치의 중복률 42% → **0%**).
전건 GitHub/HF API 실호출 검증 완료. 처리 결과는 `log.md` 의 `## [2026-09-07] ingest` 항목 참조.

---

> ⚠️ **수집기 반영 필요 5건** (자세한 내용은 actionable.md)
>
> 1. 🔴 **내용(구성) 대조 부재** *(2026-09-07 신규)*
>    raw가 [[humanlayer-skills]] 를 *"스킬 **3종**"* 이라 적었으나 **README 실측 5종**이었고, **누락된 2종 중 하나(`design-control-loop`)가 그 레포의 핵심**이었다.
>    09-06에 세운 *"raw ↔ 실측 전건 대조"* 는 **지표(숫자)에만 적용**되고 있었다.
>    → **구성 요소를 열거한 요약(N종·N개·목록)은 원문에서 개수를 반드시 재확인**한다. raw의 한줄요약은 **수집기의 요약이지 소스가 아니다.**
>
> 2. 🔴 **폐기(deprecated) 레포 탐지 부재** *(2026-09-07 신규)*
>    ⭐25,754짜리 [[openai-skills]] 가 **deprecated** 인데 신규 소스로 올라왔다. API `archived` 는 **`false`** 라 지표만으로는 못 거른다.
>    → 수집 단계에 **① README 상단 ~200자에서 `deprecated`/`no longer maintained`/`moved to`/`superseded` 검사 ② push 60일 초과 정체 플래그** 추가.
>
> 3. 🟡 **중복 필터에 이름 토큰 대조 필요** *(2026-09-07 신규)*
>    [[timesfm-3.0-pytorch]] 가 URL 필터를 통과했으나 [[TimesFM]] 이 **09-03에 이미 그 모델을 실측 기록**하고 있었다(같은 대상, 다른 표면 — arXiv/GitHub URL vs HF URL).
>    → URL 대조 + **모델·레포 이름 토큰 대조**. 히트 시 *"신규 아님"* 이 아니라 **"기존 페이지 확인 필요"** 로 플래그(라이선스가 다르면 분리가 정당함).
>
> 4. **증분 필드 폐기 — 12회 연속 미채택**
>    **절대값만 채택** 규칙 유지. 수집기에서 필드 자체를 제거할 것.
>
> 5. **`gated` 오독** *(09-04 제기, 미해결 유지)*
>    `gated` 는 **가중치 다운로드** 게이트이지 **카드 열람** 게이트가 아니다. 이번 배치 HF 모델 3건은 전부 `gated=False` 확인 후 조회했다.
>
> ✅ **이번 배치에서 확인된 것**
> - **URL 대조 필터가 작동했다** — 13건 전건 신규, 중복 **0건**(직전 배치 42%에서 해소). 단 위 3번의 빈틈 존재.
> - **raw 수집값 ↔ 실측 드리프트**: GitHub 5건 **+2~+5**(0.006~0.065%, 매우 작음) · HF 모델 3건 **완전 일치**(2,194,861 / 238,588 / 144,455) · HF 논문 업보트 4건 일치, [[Dont-Drop-Dropout]] 만 **7→8(+1)**.
>   → 09-03·09-06에 관측한 *"HF 드리프트 大"* 는 **이번엔 재현되지 않았다**. 직전 배치는 raw 수집(09-05)과 실측(09-06) 사이 **6일** 간격이었고 이번은 **당일**이라, **드리프트는 플랫폼 특성이 아니라 수집-검증 간격의 함수**일 가능성이 높다. 다음 회차 확인 대상.
