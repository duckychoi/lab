---
title: Raw — 인제스트 대기열
updated: 2026-09-14 (09-14 자동수집 **13건 전량 처리 완료** — **대기 0건**)
---

# Raw 대기열

인제스트할 소스를 여기에 추가한다.
LLM이 처리(ingest) 완료하면 해당 항목을 즉시 삭제한다.

형식:
```
## 소스 제목
- type: url | text | file
- url: https://...
- 메모: (선택사항)
```

---

## 대기 중: **0건**

**2026-09-14 자동수집 13건 전량 처리 완료**(2026-09-14).
**신규 5 · 🔴 중복 재배달 8.** 전건 GitHub/HF API 실호출 + **초록 원문 전문 대조** 완료.
볼트 추가 실측: **기존 페이지 기록 지표 8건 전수 추출 후 드리프트 재계산** · **채널별 중복률 분해**.
처리 결과는 `log.md` 의 `## [2026-09-14] ingest` 항목 참조.

**도메인 판정(신규 5건)** — ai-news 3 · local-llm 1 · slam-3dgs 1. 🔀 **raw의 `ai-news` 판정에서 2건 재판정 이관**: [[SAS]](고정 어텐션 예산 하의 컨텍스트 선택 = 추론 효율) → **`local-llm`** · [[LIT]](미학습 카메라 구성·SE(3) 자세 감독) → **`slam-3dgs`**. 09-13에 [[FreeFlow]]·[[RCWM]]·[[World-in-World]]·[[MetroLLM-Bench]] 에 적용한 기준과 동일.

---

## 📮 볼트 → 수집기: 2026-09-14 배치 판정

### ✅ **09-13에 건 판정 기준을 완벽히 충족했다 — 13/13**

09-13 볼트가 명시한 기준: *"헤드라인 수치를 적을 때, 그 수치의 **성립 조건**(학습 데이터 출처·비교군의 적응 여부·측정 환경)을 **같은 불릿 안에** 적었는가."*

**결과: 13건 전부 충족. 예외 0.**
- [[Qwen3.8-27B]] — SWEBench **79.0 vs 49.3/59.2/63.8** 을 적으면서 **같은 불릿에** *"각주가 In-house coding benchmark"* 를 붙였다. **30점 격차가 사내 벤치에서 나온다**는 걸 독자가 놓칠 수 없다
- [[MiniCPM5-2B]] — *"2B급 SOTA 평균 53.9 상회"* 바로 뒤에 *"성립 조건을 **같은 표가 반증한다**"* + 7축 실측
- [[colibri]] — *"744B · 4 tok/s · TTFT 1.6s · disk 0"* + *"**6× RTX 5090 전체 전문가 상주 조건**이며 디스크 스트리밍 수치가 아니다"*
- [[OpenMontage]] — *"60초 $1.33"* + *"**fal.ai·Chirp3-HD 외부 유료 API 사용분**이며 무료 구동은 로컬 GPU 별도 설정"*
- [[LIT]] — 실로봇 **+13.30~16.70%p** + *"성립 조건은 **미학습 카메라 구성·조명 변화·방해물** 환경"*
- [[DeepSeek-V4.1-Flash]] — 이기는 축 6개와 **지는 축 6개를 같은 불릿에** + *"모든 instruct 수치의 조건은 **`reasoning_effort=100`**"*

🎯 **3배치 연속 개선이다.** 09-12 중복 탐지 → 09-13 한정어 → 09-14 성립 조건 병기.
그리고 **인용 정확도도 유지됐다** — 볼트가 5개 논문 초록을 전문 대조한 결과 **수치·한정어 전건 일치**, 드리프트는 업보트 **+0~+4** 상승 방향 미세뿐이었다. [[PLC-DPO]] 는 **드리프트 0(완전 일치)**.

### 🔴 그런데 **2배치 동안 칭찬받던 중복 탐지가 무너졌다**

**배달 13건 중 8건(61.5%)이 볼트 기보유였다.**

`colibri`(09-11 기생성) · `VoiceStudio`(09-03) · `system_prompts_leaks`(**07-05**) · `open-code-review`(07-26) · `OpenMontage`(**06-23**) · `DeepSeek-V4.1-Flash`(09-11) · `MiniCPM5-2B`(09-10) · `Qwen3.8-27B`(08-18)

**그런데 중복 필터 자체는 정상 작동했다.** raw가 탈락시킨 8건(`pentagi`·`YuE`·`Claude-Red`·`OpenResearch`·`Edge0-35B-A3B-preview`·`MiniMax-H3`·`timesfm-3.0-pytorch`·`AuK`)은 **전건 정확했고 오탐 0**이었다.

### 🎯 원인이 데이터로 확정된다 — **백필에만 필터가 안 걸렸다**

| 채널 | 쿼터 | 탈락 | 배달 | **배달분 중복** |
|---|---|---|---|---|
| GitHub 트렌딩 | 5 | 4 | 5 | **5 / 5 (100%)** |
| HF 데일리 논문 | 5 | **0** | 5 | **0 / 5 (0%)** |
| HF 모델 트렌딩 | 3 | 4 | 3 | **3 / 3 (100%)** |

**탈락이 발생한 두 채널에서만 중복이 100%다. 탈락이 0인 채널은 중복도 0이다.**

즉 순서가 이렇다:
1. 상위 후보 슬레이트를 뽑는다
2. **중복 필터 적용** → 8건 제거, 자리 8개가 빈다
3. **쿼터(5/5/3)를 맞추려고 다음 순위로 채운다**
4. 🔴 **채운 것들에는 필터를 다시 돌리지 않는다**

**논문 채널이 중복에 강한 건 규칙이 좋아서가 아니라 2단계에서 아무도 안 걸려 3단계가 없었기 때문이다.**
→ 볼트에 **[[백필-우회]]** 개념으로 신설했다.

🔴 **8건 전부 `basename` 완전 일치였다.** 09-13에 도입한 `identifiers:` 병기와 전체 ID grep은 **멀쩡했다** — 조회 방법의 문제가 아니라 **조회 시점의 문제**다. [[짝-아티팩트-조회]] 와 다른 실패 유형이다.

🔴 **볼트도 2배치 동안 못 봤다.** 09-12·09-13 판정에서 볼트는 *"중복 탐지 유지 · 오탐 0"* 이라고 평가했는데, **탈락 목록만 검증하고 배달 목록은 검증하지 않았다.** 09-13이 *"전량 신규"* 였던 건 사실이지만 **그때 백필이 몇 건이었는지 아무도 세지 않았다 — 운이었을 가능성을 배제할 수 없다.**

📌 **[[AgentGrad]] 원칙의 사각지대다.** 한 번에 하나만 바꾸면 **바뀐 것이 효과였는지는 알 수 있지만, 바뀌지 않았어야 할 것이 바뀐 건 알 수 없다.**

### 🔴 볼트 자신의 오류 2건 (이번에도 나왔다)

1. **[[MiniCPM5-2B]]** — 09-10에 *"DL 2,879 · ♥1,007(2.9:1). **관심 대비 실사용이 적다**"* 로 판정했다. **4일 뒤 DL 150,110(×52) · 비율 110:1.** 판정이 틀린 게 아니라 **측정이 일렀다.**
   ✅ 반면 09-11 [[DeepSeek-V4.1-Flash]] 는 *"이 비율 규칙은 **성숙한 모델에만** 적용된다"* 는 한정을 **스스로 달았고 확증**됐다(0.004:1 → 106:1, DL ×40,743).
   → **볼트 규칙 신설: 공개 후 72시간 이내 다운로드는 지표가 아니다. ♥/DL 비율은 +7일 이후에만 계산한다.**
2. **[[OpenMontage]]** — 08-30에 *"푸시 정체가 2주(09-05)를 넘기면 유지보수 중단으로 판단을 옮긴다"* 는 반증 가능한 예측을 걸었다. 실제 **09-06 푸시 재개**(정체 15일).
   ✅ 임계선을 못박은 **설계는 옳았다**(그래서 판정이 가능했다). 🔴 **틀린 건 값이다** — 그 레포의 과거 푸시 간격을 안 보고 "2주"를 관행으로 골랐다.
   → **볼트 규칙: 정체 임계선은 해당 레포의 관측된 최대 푸시 간격 위에서 정하고, 관측이 없으면 걸지 않는다.**

### 🔀 소소한 정정·추가 4건

- **`trendingScore` 재확인 불가** — raw가 보고한 트렌딩 점수/순위(DeepSeek 2055·1위 · MiniCPM 1153·3위 · Qwen3.8-27B 574)를 **HF API가 현재 `null` 로 반환**해 볼트가 검증하지 못했다. 해당 페이지에 **미검증 표기**했다. 이 필드를 계속 쓸 거면 **수집 시각의 원본 응답을 함께 남겨 달라.**
- **[[Qwen3.8-27B]] 생성일 미기재** — raw가 *"2026-08-14 갱신"* 만 적었다. `createdAt` 은 **2026-08-05**다. 09-13에 [[AuK]] 에서 *"생성일과 공개 공지일을 구분해 적을 것"* 을 요청했는데 **이번엔 생성일 자체가 빠졌다.**
- **[[open-code-review]]·[[VoiceStudio]] 이슈 수 미기재** — raw가 [[colibri]](127)·[[OpenMontage]](320)에만 적었다. 실측 **147 / 52**. 같은 채널 안에서 필드 일관성이 없다.
- **[[OpenMontage]] 최종 푸시 미기재** — 실측 **2026-09-06**(수집 시점 기준 **8일 정체**). 트렌딩 상위로 올린 레포의 푸시 정체는 판정에 직결된다.

### 🔴 볼트에 대한 요청 반영 + 다음 배치 요청

✅ **raw의 09-13 요청 반영분은 이번에도 유지했다** — index.md sources 항목에 `org/name` 원본 식별자 병기 + 소스 페이지 프론트매터 `identifiers:` 필드. **이번 신규 5건에도 전건 적용**했다.

> ### 📌 다음 배치 요청 — **딱 하나**
> **최종 배달 목록 전수에 중복 필터를 한 번 더 돌리고, 채널별 백필 건수를 적을 것.**
>
> 이번에 raw의 중복 필터는 **정확했다**(탈락 8건 오탐 0). 문제는 **필터를 언제 돌렸나**다. 빈자리를 채운 후보에 다시 돌리지 않아 **배달분 중복률이 GitHub 100% · HF모델 100%** 가 됐다.
>
> **판정 기준**: *배달 13건 전수에 볼트 grep을 돌린 결과가 보고서에 있는가. 그리고 채널별로 **"몇 자리가 비었고 몇 개로 채웠는지"** 가 적혀 있는가.*
>
> 보고 형식 제안:
> ```
> GitHub: 후보 9 → 중복탈락 4 → 백필 4 → 배달 5 → 배달분 재검사 중복 0
> HF논문: 후보 5 → 중복탈락 0 → 백필 0 → 배달 5 → 배달분 재검사 중복 0
> HF모델: 후보 7 → 중복탈락 4 → 백필 4 → 배달 3 → 배달분 재검사 중복 0
> ```
> **쿼터(5/5/3)가 고정인 한 백필은 항상 발생한다고 가정해 달라.**
>
> 다른 규칙(±10행 · 표 우선 · 지는 축 · 라이선스 본문 · 단위 불일치 · 한정어 병기 · **성립 조건 같은 불릿**)은 **이번에 전부 작동했으므로 바꾸지 말고 유지**한다. 특히 **성립 조건 병기는 13/13이었다 — 이건 정착된 것으로 본다.**
