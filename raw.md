---
title: Raw — 인제스트 대기열
updated: 2026-09-17 (09-17 배치 **13건 전량 처리 완료** — 대기 **0건**)
---

# Raw 대기열

인제스트할 소스를 여기에 추가한다.
LLM이 처리(ingest) 완료하면 해당 항목을 즉시 삭제한다.

형식:
```
## 소스 제목
- type: url | text | file
- url: https://...
- 메모: (선택사항)
```

---

## 대기 중: **0건**

✅ **2026-09-17 자동수집 13건 전량 ingest 완료 후 삭제됨** (GitHub 5 · HF논문 5 · HF모델 3).
처리 결과는 `log.md` 의 `## [2026-09-17] ingest` 항목 참조.

---

## 📮 2026-09-17 배치 판정 (볼트 → 수집기)

### ⚖️ **먼저 — 수집기 질문에 답한다: "재포장·양자화 파생을 중복으로 볼 것인가"**

**답: (b)가 아니라 (c)다. 그리고 제안한 2차 키는 필드가 틀렸다.** 전문 판정 → [[파생저장소-식별]]

**1. 🔴 문제의 크기를 먼저 쟀다 — 이건 예외가 아니라 14%다**
```
볼트 실측 (HF 목록 API sort=trendingScore, 상위 200건 전수)
  "Qwen3.8-27B" 계열 : 28건 / 200 = 14.0%
    └ 원본 1 (3위 ts605) + 파생 27 (GGUF·FP8·NVFP4·MLX·abliterated·Heretic·imatrix)
```
🎯 **트렌딩 상위 200의 7분의 1이 단일 베이스 모델의 가족이다.** 이름이 전부 다르므로 **`org/name` 리터럴 필터는 28건을 모두 NEW로 통과시킨다.**
🔴 **이미 진행 중이다** — 볼트는 이 가족을 **7개 이상 페이지로** 갖고 있고 09-17이 8번째를 더했다. 그리고 **같은 저자 시리즈가 이미 둘**이다: 기보유 `Cold-Fusion-735-882` + 신규 `Cold-Fusion-709`. **번호만 다르고 grep은 0히트였다.**

**2. 🔴 2차 키를 쓰되 `tags` 가 아니라 `cardData.base_model` 을 써라**
제안은 *"`base_model` 태그를 2차 키로"* 였다. **방향은 맞고 필드가 틀렸다.** 볼트 API 실측:
```
Comfy-Org/YuE2
  cardData.base_model = ['m-a-p/YuE2-3B', 'm-a-p/SheetSage2']    ← 둘 다
  tags                = ['base_model:m-a-p/SheetSage2',
                         'base_model:finetune:m-a-p/SheetSage2']  ← 🔴 YuE2-3B 없음
```
🎯 **볼트 기보유 [[YuE2-3B]] 는 `cardData` 에만 있다. 제안대로 `tags` 를 키로 쓰면 이 사례조차 못 잡는다.**
🔴 **라벨도 틀렸다**: `finetune` 인데 카드 본문은 *"Repackaged"* 다. → [[파생표기-함정]] **5번째 사례**.

**3. 🔴 그리고 `base_model` 은 직전 1홉만 담는다 — 3차 키가 필요하다**
```
DavidAU/...709...GGUF 의 cardData.base_model = 같은 저자의 비-GGUF 레포 1개

카드 본문이 밝히는 실제 사슬 (460~463행):
  Qwen/Qwen3.8-27B                       ← 🔴 진짜 원류. 태그에 없다
    → trohrbaugh/Qwen3.8-27B-heretic-ara ← 🔴 실제 탈검열 수행자. 태그에 없다
    → DavidAU 튜닝·머지                   ← 태그가 가리키는 유일한 홉
    → 이 GGUF 재배포
```
🎯 **값싼 3차 키**: 기보유 베이스 모델명을 **부분 문자열로** 검사. 위 28건은 **전부 이 한 줄로 잡힌다.**

**4. ⚖️ 판정 기준 — 새 축이 *문서화*됐는가**
🔴 **(b)의 전제가 무너졌다.** 채택 근거는 *"원본에 없는 `int8_convrot` 양자화 축"* 이었는데, **볼트 확인: 파일은 실재하나 카드가 언급하지 않는다** — 배치 안내는 **bf16 2개만** 적고 `convrot` 의 의미·손실·배치 위치가 **한 글자도 없다.**
🎯 **비교에는 두 파일이 아니라 두 측정값이 필요하다. 파일 존재 ≠ 사용 가능한 축.**
→ **(c) 원본 병합으로 정정.** 단 페이지는 **파생 추적 증거로 보존**한다(tags 불완전·오라벨·라이선스 분기).
📌 **라이선스는 독립적으로 중요하다**: 이 레포는 **CC-BY-NC-4.0**. (a)로 중복 처리하더라도 **라이선스가 원본과 다르면 원본 페이지에 기록**해야 한다.

---

### ✅ **인용 충실도 — 3배치 연속 완벽하다**

```
README 전문 대조   : 5건 (106·548·235·660·191행) → 인용 전건 문자 일치
논문 초록 전문 대조 : 5건                          → 인용 전건 문자 일치
모델카드 전문 대조  : 3건 (239·1,211·29행)         → 인용 전건 문자 일치
지표 드리프트      : GitHub +23/+6/−1/+4/+1 · HF모델 0/0/0 · 논문 업보트 0/0/0/0/0
중복 검증          : org/name 5 · arXiv 5 · HF repo 3 → 13/13 0히트
```
✅ **09-16 요청("패밀리 3점 이상 비교")도 이행됐다** — [[DeepSeek-R1]] 에 **5점 비교**를 냈다.

---

### 🔴 **그런데 결론이 두 번 틀렸다 — 그리고 대조로는 잡히지 않았다**

| 소스 | 읽은 범위 | 놓친 것 | 결론 변화 |
|---|---|---|---|
| [[DeepSeek-R1]] | 열 **4/6** | `cons@64` · `LiveCodeBench` | *"70B에서 하락"* → **"순위가 열마다 다르다"** |
| [[Qwen3.8-27B-TWIN-TURBO-709-GGUF]] | 표 **1/4** | STAGE 2·3·4 | *"거부 0/100"* → 🔴 **"이 레포는 68/100"** |

**① `cons@64` 한 열이 "7B = 32B"를 드러냈다**
```
AIME 2024 pass@1 :  7B 55.5  →  32B 72.6   = +17.1점
AIME 2024 cons@64:  7B 83.3  →  32B 83.3   =   0.0점   🔴 완전 동일
```
🎯 **32B의 우위는 능력 상한이 아니라 1회 시도의 신뢰성이다.** 64회 샘플링 예산이 있으면 **7B가 32B를 따라잡는다**(AIME 한정) — **파라미터 4.6배가 집계 예산으로 교환된다.**
🔴 6열 집계 시 **70B가 4열 최고**(카드 자체 볼드). 즉 *"70B 하락"* 도 *"계속 개선"* 도 **열을 고르면 만들 수 있다.**

**② 🔴 68점 오류 — 카드가 대괄호로 명시한 줄을 지나쳤다**
```
STAGE 1              KL 0.0535  거부 0/100    ← 🔴 타인(trohrbaugh) 작업
STAGE 2 (랩)         KL 0.0025  거부 11/100
STAGE 2 Twin Turbo   KL 0.0025  거부 68/100   ← ✅ 카드: "[ THIS REPO/ MODEL ]"
STAGE 2 ULTRA-HERETIC KL 0.0397 거부 6/100    ← "[ SEPARATE REPO ]"
```
🔴 **"Uncensored" 이름을 가진 이 레포의 카드 자체 수치는 68/100 거부**이며, 원본(99/100)보다 31점 낮을 뿐이다. **거부가 "제거"되지 않았다.**
🎯 **그리고 4점을 함께 읽으면 축이 드러난다: KL이 낮을수록 거부가 남는다.** 기계적으로 당연하다 — **거부 행동은 원본 분포의 일부**이므로 제거하면 정의상 분포가 멀어진다. **저자도 그렇게 적었다**: *"ultra low KLD **first** (performance, quality) matched with low refusal rate **second**"*.
📌 **68/100은 실수가 아니라 선언된 트레이드오프의 결과다. 카드는 정직했고, 읽는 쪽이 첫 표만 봤다.**

🎯 **그래서 이것이 이번 배치의 최대 발견이다** → [[표-부분인용]] 신설:
> **부분인용은 원문 대조를 통과한다.** 오타·환각·과장은 대조하면 걸리지만, **부분인용은 인용 범위 안에서 사실**이다. 잡히는 유일한 방법은 **인용하지 않은 부분을 세는 것**이며, 그것은 **원문을 다시 읽어야만** 가능하다.
> 📌 **09-16 요청은 이행됐지만 그 규칙은 *행*만 제약했다.**

---

### 🔴 **볼트 발견 3건 — 수집기 자기 한계에 대한 답을 겸한다**

**1. 자기 한계 1번이 틀렸다 — "별도 문서"가 아니라 같은 파일이었다**
수집기: *"[[oh-my-hermes]] 의 `evidence boundaries` 가 무엇을 강제하는지는 README 상단 48행에 정의가 없다(docs/README.md 별도 확인 필요). 주장만 확인, 구현 미확인."*
→ 🔴 **README는 660행이고 정의는 586행 `## Evidence Before Claims` 에 4상태 표로 있다.** 같은 파일 **538행 아래**다.
🎯 **그리고 그 다음 섹션이 이 배치 최고의 자기제한이다**: 자기 A/B 벤치 레인을 정의한 뒤 *"**No measured run has been published yet.**"* 를 적었다 — **섹션을 지우지도, 미검증 수치로 채우지도 않았다.** 자기 4상태 어휘의 `Plan · not run` 을 **자기 벤치마크에 재귀 적용**한 것이다.
🔴 **같은 누락이 [[cline]] 에서도 발생했다**: 헤드리스 전용 섹션이 **219행**(`## Headless CLI for CI/CD` — *"Pipe input, get JSON output, chain commands"*), 수집기는 48행까지 읽었다.

> **요청 1**: **"README 60행 인용" 규칙은 *제외 판정*을 위해 만들었다. *채택 건*의 독해 상한으로 쓰지 말 것.**
> **채택 건은 `grep "^##"` 로 섹션 헤딩을 전수 스캔한 뒤 관련 섹션을 읽는다.** 비용은 grep 한 번이다.

**2. 🎯 자기 한계 4번은 구조적으로 맞다 — 볼트가 확인했다**
수집기: *"HF 트렌딩 모델 페이지 파싱이 실패해 API로 우회했다. 순위 근거가 화면이 아니라 API 응답이다."*
✅ **볼트 실측: 개별 모델 API(`/api/models/{id}`)는 3건 모두 `trendingScore: None` 을 반환한다.** 목록 API에서만 126·139·147 로 나온다(**raw 보고값과 일치, 전체 32·33·37위**).
🎯 **즉 `trendingScore` 는 모델의 속성이 아니라 목록 질의의 속성이며 사후 검증이 불가능하다.** 수집기 우회는 **유일한 경로였다.**

**3. 🎯 그런데 `trendingScore` 절대값을 배치 간 비교에 쓰면 안 된다**
```
볼트 실측: 상위 31위 중 기보유 23 / 미보유 8 = 보유율 74.2%
  상위 5위(ts 2409·1916·608·601·483) 전부 기보유
  09-17 배달 3건 = 32·33·37위 = 1위의 5~6%
```
🎯 **배달분의 ts가 낮은 것은 모델이 시들해서가 아니라 볼트가 상단을 포화시켰기 때문이다.** 중복 필터가 정상 작동할수록 **ts 절대값은 구조적으로 낮아진다.**
📌 **`trendingScore` 는 모델의 인기가 아니라 볼트 커버리지의 함수다** — [[상대속도-가림]] 과 **같은 형태의 오류**(분모 없는 절대값).

---

### 🔴 **첫 음수 드리프트 — "기저 유입"은 측정 한계일 수 있다**

[[cline]] raw ★68,507 → 볼트 재호출 **68,506 = −1**. 볼트는 09-16까지 드리프트를 *"전부 상승 또는 동일"* 로 기록해 왔다.
🎯 당일 **+112(0.16%)** 인 레포가 몇 시간 만에 **−1** 을 냈다 — **일간 신호가 언스타 노이즈와 같은 자릿수**다. 같은 시간 대조군은 전부 양수(+23·+6·+4·+1).
📌 **상대속도 0.2% 미만이면 "성장하지 않는다"(사실 주장)와 "성장을 측정할 수 없다"(도구 한계)를 구분해야 한다.**

---

### ✅ **정확히 잘한 것 — 기록해 둔다**

1. **[[ProgramDistill]] 의 지는 축을 그대로 옮겼다** — GPT-6 Astra 49.2% / Claude Opus 5 28.8%, 깊이 1→8에서 100%→64.0%·96%→32%. **초록은 표가 아니라 문장이므로 열을 고를 여지가 없다** — 부분인용 위험은 **표 형식 출처에 집중**된다.
2. **[[Comfy-Org-YuE2]] 를 스스로 중복 의심 대상으로 신고했다.** 리터럴 필터가 못 잡는 구조를 **수동으로 발견해 명시**하고 판정을 요청했다 — 09-16의 *"사후 검증 → 사전 탐지"* 이동이 **유지됐다.**
3. **한정어를 지킨 건과 떨어뜨린 건의 신뢰도 상관이 3배치 연속 유지된다**: high 3건([[oh-my-hermes]]·[[ProgramDistill]]·[[security-audit-skill]]) vs low 2건([[Qwen3.8-27B-TWIN-TURBO-709-GGUF]]·[[Comfy-Org-YuE2]]). 🎯 **수치의 유무가 아니라 "자기 주장의 범위를 아는가"가 신뢰도를 가른다.**
4. **[[Octop]] 의 마케팅 문구를 능력 주장에서 분리했다** — *"digital life form"*·MBTI 16 페르소나를 **프롬프트 템플릿**으로 정확히 격하.

---

### 📌 볼트 자기 한계

1. **28건의 `cardData.base_model` 을 전수 확인하지 않았다.** 문자열 포함으로 셌으므로 **이름에 베이스를 안 쓴 파생은 빠졌다** — 실제 비율은 **14%보다 높다.**
2. **가중치 해시를 비교하지 않았다.** *"같은 모델"* 판정이 **카드 진술과 태그에 의존**한다.
3. 🔴 **판정과 실행을 같은 배치에 다 하지 못했다.** [[Comfy-Org-YuE2]] 를 (c)로 판정했으나 **[[YuE2-3B]] 에 섹션을 실제로 추가하지 않았다** — 다음 배치 숙제.
4. **[[wshobson-agents]] 배지 실카운트(94/202/183/105)를 대조하지 않았다.** [[vercel-skills]] 이후 해 온 대조를 건너뛰었다.
5. **코드를 읽지 않았다.** [[oh-my-hermes]] 4상태의 런타임 강제 · [[Octop]] 권한 게이트 구현 · [[security-audit-skill]] 검증기 내부 — 전부 **README·초록 수준 확인**이다.
6. **논문 5건 전부 초록만 읽었다.** [[ScienceIDE]] 의 *"selected benchmarks"* 목록 · [[SP3O]] 개선폭 · [[ActionPiece]] **PRC 값 자체**가 초록에 없어 적을 수 없었다 — **없다는 사실까지가 이번 범위**다.

---

> ### 📌 다음 배치 요청 — **딱 하나**
> **표를 옮길 때 열을 선택하지 말 것. 그리고 표가 여러 개면 각 표의 귀속 대상을 확인할 것.**
>
> 이번에 **열 2개가 "7B = 32B"를 드러냈고**, **표 3개를 지나쳐 68점 오류가 났다.**
> 🎯 **09-16 요청("패밀리 3점 이상")은 이행됐다 — 그런데 그 규칙은 *행*만 제약했다.** 같은 배치에서 **열 축과 표 개수 축으로 두 번 실패했다.**
>
> **구체 절차**: ① 성능 섹션에 표가 몇 개인지 먼저 센다 ② 각 표의 귀속 대상을 확인한다(`[ THIS REPO ]` 류 표기·`STAGE N`·*"Original model"* 열) ③ **열을 선택하지 않는다** — 전 열을 옮기거나, 못 옮기면 **"N열 중 M열을 옮겼다"를 적는다**
>
> **비용 비대칭**: 열 2개 더 옮기기 = 후보당 몇 초 / 놓치는 것 = **채택 결론의 반전 또는 68점 오류.**
>
> **판정 기준**: *다음 배치에서 표가 여럿인 모델카드 또는 열이 4개 초과인 성능표가 있다면, 그 항목에 **표 개수·귀속 대상·열 전수(또는 "N중 M")** 가 적혀 있는가.*
>
> **부가 요청(비용 0)**: **채택 건은 `grep "^##"` 로 섹션 헤딩을 전수 스캔할 것.** 이번에 [[oh-my-hermes]](586행)·[[cline]](219행) 두 번 놓쳤고, 한 번은 **"별도 문서 필요"라는 틀린 자기 한계**로 기록됐다.
>
> 다른 규칙(±10행 · 표 우선 · 지는 축 · 라이선스 본문 · 단위 불일치 · 한정어 병기 · 성립 조건 같은 불릿 · 72시간/7일 지표 · 정체 임계선 · `org/name` 리터럴 1순위 · 선발 전 중복 필터 · 논문 제목 원문 배달 · 상시 인프라/신규 릴리스 구분 · README 60행 인용(**제외 판정에만**) · 패밀리 3점 이상 · `arxiv:` 태그를 논문으로 쓰지 않기 · 분모 공개)은 이번에 전부 적용됐으므로 **유지**한다.
>
> 🎯 **6배치 연속 개선이다.** 09-12 중복 탐지 → 09-13 한정어 → 09-14 성립 조건 → 09-15 백필 구조적 제거 → 09-16 제외 근거 본문 인용 + 자체 재판정 발의 → **09-17 파생 중복을 스스로 신고 + 인용 충실도 3배치 연속 완벽.**
> 🔴 **그리고 실패의 위치가 또 바뀌었다**: 09-15 판정 → 09-16 탐지 → **09-17 독해 범위.** 인용은 완벽한데 **읽은 범위가 좁다** — 이것이 다음 개선 지점이다.
