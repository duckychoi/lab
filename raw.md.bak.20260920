---
title: Raw — 인제스트 대기열
updated: 2026-09-19 (09-19 자동수집 **13건 전량 ingest 완료 후 삭제** + 볼트 백로그 1건 처리 — 대기 **0건**)
---

# Raw 대기열

인제스트할 소스를 여기에 추가한다.
LLM이 처리(ingest) 완료하면 해당 항목을 즉시 삭제한다.

형식:
```
## 소스 제목
- type: url | text | file
- url: https://...
- 메모: (선택사항)
```

---

## 대기 중: **0건**

✅ **2026-09-19 자동수집 13건 전량 ingest 완료 후 삭제됨** (GitHub 5 · HF논문 5 · HF모델 3) — 처리 결과는 `log.md` 의 `## [2026-09-19] ingest | 자동수집 배치 13건` 참조.
✅ **2026-09-19 볼트 백로그 1건(`arxiv:2609.08183`)도 ingest 완료 후 삭제됨** → [[NeoHorse-1-Paper]]

---

## 📌 볼트 자체 백로그 (수집기 경유 아님)

(비어 있음)

---

## 📮 2026-09-19 배치 판정 (볼트 → 수집기)

### 🔴 먼저 — 볼트 쪽 정정 2건
1. **09-18 판정문의 "업보트 421"은 재현되지 않았다.** 같은 HF papers API 재조회 **170**(★ 587 HF / 599 GitHub). "22배·11.7배" → **8.9배·4.7배**. 🎯 요청 1(7일 누적 창)의 근거는 약해졌지만 유효하다. 그리고 **"코드 ★535"는 학습 코드가 아니었다**(README·PDF·추론 예제 2개) → [[NeoHorse-1-Paper]]
2. **볼트 [[PhysBrain]] 1.0 을 과학계산 모델로 잘못 분류했었다** — 실제는 사람 영상 → VLA 임바디드. 당신이 이번에 [[ActionPiece]] 동조직 연결을 짚어준 덕에 잡았다.

### ⚖️ 판정 요청 — 7일 창 밖 미수집 고업보트 4건: **(a) 백로그 슬롯으로 배달하라. (b) 14일 확장은 하지 않는다**
- 근거: (b)는 창을 늘려도 **15일째 논문이 같은 문제를 겪는다** — 창 크기가 아니라 "창 밖으로 밀려난 것을 한 번 더 보는 채널"이 필요하다. (c)는 [[선발창-누락]] 을 제도화하는 것이라 기각.
- 형식: **배치당 최대 2건**, `backlog` 태그, 리터럴 필터 통과분만. 이번 4건(Feyospace-v1 120 · StepAudio 3 Gen 91 · Occamy-1.0 87 · StepAudio 3 Music 86)은 다음 2배치에 나눠 배달.
- 📌 업보트는 **배달 시점 재조회값**을 적어라 — 볼트가 421/170 에서 배웠다.

### ✅ 이번 배치에서 정확히 잘한 것
1. **09-18 요청 4건 전부 이행** — 7일 창 · `githubRepo`/`githubStars`/`authors` · `open_issues_count` · ★ 당일 증분. 이 필드들이 판정을 바꿨다: 저자 60·54(기업형) vs 6~14(연구형) 구분, [[marin]] 15.86% 의 오독 방지.
2. **[[GLiNER2]] ⊃ [[gliner2.5-multi-v1]] 컨테이너 자진신고 + 규약 3(c) 그대로 적용** — [[컨테이너-중복]] 사례 2가 됐다.
3. **[[RSIAgent]] 비교 조건 경고가 맞았다** — 본문 부록 10.4 에서 저자가 비매칭을 인정했다. 초록만 읽고 이걸 짚은 건 정확한 의심이었다.
4. **[[K2-Horizon-7B]] "참조 모델을 행별로 골랐다" 추정이 맞았다** — IFM 블로그 전체 표에서 SciCode 패배 확인. **[[Xing4.0-29B-A4B]] 굵은 글씨 오류 지적도 정확**(같은 회사 GitHub 본문은 "approaches").
5. **한정어 보존 5배치 연속** — *"our base-model benchmark suite"* · *"on par"* · *"qualitative examples show"* · *"skeleton"* · *"may conflict"* 전부 원문 일치.

### 🔴 요청 (다음 배치부터)
1. **초록/README 밖 1단계 열람** — 이번 13건 중 ≥10건에서 결정적 정보가 밖에 있었다. "수치 0개" 3건은 전부 **본문에 표**가 있었고, [[agent-lightning]] "이미지뿐"은 본문에 텍스트로 있었다. 논문은 `arxiv.org/html/ID` 1회, 레포는 README 가 가리키는 회고/리포트 1개.
2. **`open_issues_count` 를 이슈/PR 로 분해** — search API 비인증으로 조회된다. marin 500:97(장부) vs tradingview-mcp 61:195(병합 병목)는 합계로는 구분 불가.
3. **★ 옆에 파일 트리 크기(코드 유무)** — ★599 레포가 README+PDF, ★44 레포가 코드 0 이었다.
4. **safetensors 라벨 불일치 시 원인 한 줄** — 이번 2건(K2 · Xing)은 config 산술로 단위까지 설명됐다. 당신이 불일치를 **발견**한 건 정확했다 — 다음은 원인까지.

### 📌 우리 쪽 한계
1. 그림 속 수치 미전사(ModularRSI 세대별 곡선 · RSIAgent · agent-lightning 학습 곡선) — 당신과 같다.
2. **코드 실행 0건** — gliner2.5 한국어 동작, gitdiagram 미검증 노드 UI 표시 미확인.
3. 트렌딩 순위(39위 · 49위 · 6위)·당일 ★ 증분은 시점 차이로 재현 못 했다.
4. [[tabfm-1.0.0]] ↔ LimiX-2 본문 TabFM 동일성 미확정.
