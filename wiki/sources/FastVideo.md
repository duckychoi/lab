---
title: "FastVideo — '50배 가속'의 근거표가 레포 안에 0개다"
type: source
domain: video-saas
tags: [video-saas, github, video-generation, distillation, sparse-attention, dit, dmd2, 근거부재, 파생저장소-식별, 원본-파생-역전]
created: 2026-09-18
updated: 2026-09-20
sources: []
reliability: medium
---

# FastVideo

> [!update] 2026-09-20 병합 — `FastVideo/FastVideo-FastH3-Comfy` (판정 **(c) 병합**, 독립 페이지 생성 안 함)
> 수집기가 HF 모델로 별도 배달한 **FastVideo-FastH3-Comfy** 를 [[파생저장소-식별]] 판정 절차에 따라 **이 페이지로 병합**한다 — *"새 축이 문서화됐으면 (b)독립채택 · **파일만 있으면 (c)병합** · 순수 재포장이면 (a)중복"*.
>
> 🔴 **수집기 판정 정정: "신규 능력 없음 · 가중치를 ComfyUI 폴더 구조에 맞춰 재배치한 배포 편의 패키지" 는 파일명과 어긋난다.** 볼트 파일트리 실측:
> - `diffusion_models/fastvideo_fasth3_8step_v2_**pruned**_bf16.safetensors`
> - `diffusion_models/fastvideo_fasth3_8step_v2_pruned_**int8_convrot**.safetensors`
> - `text_encoders/qwen3vl_32b_minimax_h3_{bf16, **int8_convrot**, **nvfp4_awq**}.safetensors`
> - `vae/minimax_h3_{audio_vae_fp32, video_vae_fp16, video_vae_**int8_convrot**}.safetensors`
>
> **`pruned` · `int8_convrot` · `nvfp4_awq` 는 재배치가 아니라 변형이다.** 순수 재포장이면 (a)여야 하는데 변형이 있으므로 (a)가 아니고, **README 전문이 파일 배치 안내뿐**이라 문서화가 없으므로 (b)도 아니다 → **(c) 병합.**
> 🎯 **`int8_convrot` 가 두 번째 조직에서 나왔다.** 볼트 09-19 판정 *"[[Comfy-Org-YuE2]] (b)→(c) 정정 — 채택 근거였던 `int8_convrot` 이 카드에 미문서화"* 와 **같은 포맷, 같은 미문서화, 다른 조직**(Comfy-Org → FastVideo). 🔴 **재포장 계층에서 반복되는 무명 포맷**이며 볼트는 아직 이것이 무엇인지 모른다.
>
> 🔴 **그리고 여기서 [[파생저장소-식별]] 의 탐지 방법이 깨진다** — 개념은 *"`cardData.base_model` 을 읽어야 한다 · base_model은 **1홉만** 기록한다"* 고 적었다. 실측:
> - 실제 사슬: `MiniMaxAI/MiniMax-H3` → `FastVideo/FastVideo-FastH3-8-Step-V2` → `FastVideo-FastH3-Comfy`
> - 🔴 **Comfy 의 `cardData.base_model` = `MiniMaxAI/MiniMax-H3`** — **직전 부모를 건너뛰고 조부모를 가리킨다.**
> - ✅ 반면 **README 본문은 직전 부모를 정확히 적는다**: *"Original model repository: https://huggingface.co/FastVideo/FastVideo-FastH3-8-Step-V2"*
> 🎯 **기계판독 필드가 틀리고 산문이 맞다** — [[Comfy-Org-YuE2]](tags가 틀리고 cardData가 맞았다)와 **정반대 방향**이다. **1홉만 기록하는 게 아니라 "틀린 1홉"을 기록할 수 있다.**
> 🔴 **`base_model_relation` 미설정** → 태그가 `base_model:finetune:MiniMaxAI/MiniMax-H3` 로 떨어진다(**실제는 재포장**). 같은 배치 [[Swift-Qwen3.8-27B-GGUF]] 는 `base_model_relation: quantized` 를 **명시**했다 → **탐지 키를 `base_model` 에서 `base_model_relation` 으로 올려야 한다.**
>
> 🔴 **채택이 원본을 95.6배 앞선다**(볼트 실측, 2026-09-20): 재포장 **132,886** 다운로드(수집기 101,020) vs 원본 `FastVideo-FastH3-8-Step-V2` **1,390**. 좋아요는 96 vs 84. → [[원본-파생-역전]] 의 두 번째 사례이자 **가장 극단**([[Swift-Qwen3.8-27B-GGUF]] 12.5배).
> 🎯 **이 사실 자체가 이 페이지에 대한 답이다**: 위 *"기능 벤치마킹"* 항목이 *"4090에서 FastH3 8-Step V2 를 돌려 E2E 시간을 재라"* 고 적었는데, **실제로 사람들이 돌리는 것은 8-Step-V2가 아니라 ComfyUI 재포장본**이다(95.6:1). **실측 대상을 Comfy 패키지로 바꾸는 것이 맞다.**
>
> 🔴 **미해결 질문 2개는 여전히 해소되지 않았다** — Comfy 카드에 **벤치·품질 수치 0개**다. *"VSA 80%의 품질 비용"* 과 *">50x 의 베이스라인"* 은 그대로 빈칸이다. 그리고 **`pruned` 가 원본 대비 무엇을 얼마나 잘라낸 것인지도 어디에도 없다** — 빈칸이 하나 늘었다.
> 원본 실측: `FastVideo/FastVideo-FastH3-Comfy` 좋아요 96 · created 2026-09-08 · lastMod 2026-09-16 · license `other`(minimax-h3-community-license-agreement) · `library_name: diffusion-single-file` · 파일 10개

> [!insight] 핵심 인사이트 — 두 기법을 곱한 것이 이 프레임워크의 논지다
> **희소증류(sparse distillation)** = **DMD2 단계별 증류**(스텝 수를 줄인다) × **Video Sparse Attention**(스텝당 연산을 줄인다). 두 축이 **직교하므로 곱해진다** — 여기서 *">50배 디노이징 가속"* 주장이 나온다.
> 학습까지 커버한다: FSDP2 · 시퀀스 병렬 · 선택적 활성화 체크포인팅.
> **최신 릴리스(2026-09-15)**: FastH3 8-Step V2 — [[MiniMax-H3]] 에서 증류한 **8-forward data-free DMD2** 체크포인트, **VSA 80%** 적용. 🎯 **`data-free`** 가 핵심 단어다 — 원본 학습 데이터 없이 증류한다.
> **하드웨어 범위(카드 명시)**: H100·A100·**4090** 지원, Linux/Windows/macOS. Apple Silicon은 MLX 경유(FastMetal-QAD 1.3B/5B/14B).

> [!warning] 🔴 수치의 출처를 구분한다 — **README 안에 성능 대조표가 0개다**
> *">50x denoising speedup"* 과 *"5s 영상 1.8s E2E"* 는 **README 본문·블로그 주장**이며 **README 내 비교표로 제시된 것이 아니다.**
> 🔴 **208행 전체에서 성능 대조표 0개** — 존재하는 표는 **증류 레시피/데이터셋 링크표 1개**뿐이다.
> 🎯 **그래서 두 수치를 서로 대조할 수 없다.** ">50x 디노이징"은 **부분 구간**(디노이징만)이고 "5s→1.8s"는 **E2E**다 — 디노이징이 50배 빨라져도 VAE 디코딩·인코딩이 남으므로 **두 수치는 같은 분모를 쓰지 않는다.** 어느 쪽도 베이스라인·해상도·스텝수가 명시되지 않았다.
> 📌 **[[단위-불일치]] 의 영상 도메인 첫 사례다**: 지금까지 이 개념은 파라미터 수(552B↔763B)에서 나왔다. 여기서는 **가속 배수의 분모**가 다르다 — *"무엇을 무엇으로 나눈 배수인가"* 가 명시되지 않으면 배수는 숫자가 아니다.
> → 인용 시 **반드시 "README 대조표 없음 · 외부 블로그 근거"를 병기**한다. **표가 없다는 사실까지가 이번 확인 범위다.**

> [!note] 📌 볼트 실측 (2026-09-18, GitHub API)
> ★**4,467**(raw **완전일치**) · fork **465** · **Apache-2.0** · created **2024-10-24**(1년 11개월) · pushed 2026-09-17T02:58 · **open issues 192**
> **open issues / ★ = 4.30%** — 🔴 배치 2위(1위 [[harness-sdk]] 10.39%). 중위값 1.08%의 **4배**다. 🎯 **영상 생성 프레임워크는 하드웨어·드라이버·의존성 조합이 폭발하므로** 이 비율이 높은 것은 설명 가능하다(추정). 다만 **확인은 안 했다.**
> **fork:★ = 1:9.6** — 연구 프레임워크 대역. 체크포인트를 받아 쓰는 사용자는 fork하지 않는다.
> ⚠️ **★ 증가량이 raw에 없다** — 수집기가 당일 증분을 적지 않았다. 따라서 **상대속도를 계산할 수 없다.** 배치 5건 중 유일한 결측이다.

## 도메인별 추출 (video-saas)

- **기능 벤치마킹**: 🎯 **내 영상 파이프라인에 가장 직접적인 배치 항목이다.** `/reat-render` 계열 작업에서 병목은 항상 **생성 시간**이다. 8-step data-free 체크포인트는 *"품질을 조금 내주고 스텝을 8로 고정"* 하는 거래이고, **4090 지원이 명시**돼 있다는 점이 결정적이다(H100 전용이면 검토 대상도 아니다).
  - 🔴 **난이도**: 낮지 않다. DMD2 증류 체크포인트를 쓰려면 **[[MiniMax-H3]] 계열 모델에 묶인다.** 내가 쓰는 모델이 아니면 증류를 직접 해야 하고 그건 학습 비용이다.
- **크리에이터 인사이트**: **갭이 명확하다** — 사용자가 원하는 건 "빠른 생성"인데 프레임워크가 주는 건 "빠른 디노이징"이다. E2E 시간에는 **업로드·VAE·인코딩·후처리**가 들어간다. *"50배"* 를 본 사용자의 기대와 실제 체감은 **같을 수 없다.**
- **프롬프트 패턴**: 해당 없음(추론 프레임워크, 프롬프트 기법 아님).
- **워크플로우**: 증류 레시피 표 1개가 유일한 구조적 정보 — **레시피/데이터셋 링크**만 있고 end-to-end 시간 분해는 없다.
- **디자인 레퍼런스**: 해당 없음.
- **경쟁 우위 빈틈**: 🎯 **여기가 진짜 발견이다.** 이 레포는 *"증류 × 희소어텐션을 곱한다"* 는 조합을 제시하면서 **그 조합의 이득을 분해하지 않았다.** 증류 단독 이득 · VSA 단독 이득 · 곱의 이득이 **각각 얼마인지 표가 없다.** → **직접 재면 그게 차별점이 된다.** 동시에 이건 [[Harness-Design-Empirical]] 이 하네스 축에서 한 일(**컴포넌트 단위 대조**)과 **같은 종류의 빈틈**이다 — 영상 가속 쪽에는 아직 그 논문이 없다.

> [!action] 당장 할 것
> **4090에서 FastH3 8-Step V2 를 돌려 E2E 시간을 직접 잰다.** 그리고 **디노이징 시간과 총시간을 따로 기록한다** — 레포가 하지 않은 분해가 그것이다. 🔴 단 전제 확인 먼저: 내 파이프라인이 [[MiniMax-H3]] 계열을 쓰지 않으면 **체크포인트를 그대로 쓸 수 없다.**

> [!question] 미해결 질문
> **VSA 80% 의 품질 비용이 어디에도 없다.** 어텐션의 80%를 희소화하면 무엇이 깨지는가 — 시간 일관성? 미세 텍스처? **카드에 품질 지표가 0개**다.
> 그리고 **">50x" 의 베이스라인이 무엇인지** 특정 불가. 50-step 원본 대비 8-step이면 6.25배이므로, 50배는 **VSA 이득까지 곱한 값**이어야 산술이 맞는다 — 그러면 VSA가 8배를 낸다는 뜻인데 **그 수치는 없다.** 🔴 **볼트는 이 산술이 닫히지 않는다고 기록한다.**

## 관련 페이지
- [[단위-불일치]]
- [[MiniMax-H3]]
- [[표-부분인용]]
- [[harness-sdk]]
- [[Harness-Design-Empirical]]
- [[AI-영상-생성-2026]]
- [[한정어-탈락]]
- [[검사가능성-공사]]
- [[파생저장소-식별]]
- [[원본-파생-역전]]
- [[Comfy-Org-YuE2]]
- [[Swift-Qwen3.8-27B-GGUF]]
- [[TensorRT-LLM]]

## 원본
- 출처: https://github.com/hao-ai-lab/FastVideo
- 볼트 실측(2026-09-18, GitHub API): ★**4,467**(raw **완전일치**) · fork **465** · **Apache-2.0** · created 2024-10-24T02:11:32Z · pushed 2026-09-17T02:58:55Z · **open issues 192** · archived false
- raw 대비: **드리프트 0**. 볼트 추가 = **">50x"와 "5s→1.8s"가 같은 분모를 쓰지 않음을 명시하고 [[단위-불일치]] 의 영상 도메인 첫 사례로 승격** · **50배 산술이 닫히지 않음**(8-step만으로 6.25배 → VSA가 8배를 내야 하는데 수치 없음) · open issues 4.30% = 배치 2위 · **★ 증분 결측 = 상대속도 계산 불가**(배치 유일)
- 신뢰도: ⭐⭐ (지표 완전일치 · 그러나 핵심 성능 주장의 근거표가 레포 내 0개이고 외부 블로그 의존 · 품질 비용 수치 전무)
