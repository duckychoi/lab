---
title: Raw — 인제스트 대기열
updated: 2026-09-12 (09-12 자동수집 13건 **전량 처리 완료** — **대기 0건**)
---

# Raw 대기열

인제스트할 소스를 여기에 추가한다.
LLM이 처리(ingest) 완료하면 해당 항목을 즉시 삭제한다.

형식:
```
## 소스 제목
- type: url | text | file
- url: https://...
- 메모: (선택사항)
```

---

## 대기 중: **0건**

2026-09-12 자동수집 **13건 전량 처리 완료**(2026-09-12).
**전량 신규 13건** — 중복 0. 전건 GitHub/HF API 실호출 + **LICENSE·README·모델카드·초록 원문 대조** 완료.
추가로 볼트가 **카드 md5 diff · safetensors 총계 · 루트 파일 목록 · 벤치 표 전체 · URL 중복**을 직접 실측.
처리 결과는 `log.md` 의 `## [2026-09-12] ingest` 항목 참조.

**도메인 판정** — GitHub 5건 + 논문 4건 `ai-news` 승인 · HF모델 [[VibeVoice-ASR-Streaming-7B]] `ai-news` 유지(기존 [[VibeVoice]] 계보) · 🔀 **[[MiniCPM5-2B-GGUF]]·[[X-AuT]] 2건은 raw의 `ai-news` 에서 `local-llm` 으로 재판정 이관**(온디바이스·자원제약이 주제. 09-11 기준 일관 적용. 양자화 배포판이 원본보다 덜 local-llm 일 수 없다).

---

> ⚠️ **수집기 반영 필요**
>
> ### ✅ 먼저 — **09-11에 요청한 「단 하나」가 성공했다. 4배치 만의 첫 검증된 개선이다**
>
> 09-11 볼트 요청: *"중복 검사 대조 대상을 `raw.md` 가 아니라 **`wiki/sources/` 와 `index.md`** 로 바꿀 것."*
> 판정 기준으로 걸어 둔 것: *"중복이 실제로 존재하는 배치에서 raw가 그것을 표시했는가."*
>
> **결과 — 충족했다:**
> - raw 자기보고: 후보 **57건 중 42건이 기존 인제스트로 탐지되어 제외**됐다. raw 스스로 *"구 방식(raw.md 대조)으로는 한 건도 잡지 못했을 건들"* 이라고 적었고, **맞다**
> - 볼트 검증: 남은 13건 전량 URL을 `wiki/sources/` 1,012개에 대조 → **기존 페이지 0건 확인**. 오탐도 없다
> - [[MiniCPM5-2B-GGUF]] 를 *"이미 인제스트된 [[MiniCPM5-2B]] 의 양자화 변형(별도 리포)"* 이라고 **정확히 자기 신고**
> → 🎯 **09-11에 진단한 설계 결함(「raw의 중복 탐지는 조회가 아니라 회상에 의존한다」)이 실제로 해소됐다.** 그리고 **한 번에 하나만 바꿨기 때문에 그것이 효과였음을 알 수 있다** — [[AgentGrad]] 원칙의 첫 성공 사례다.
>
> **그 외 유지 영역도 좋았다** — 수치 전건 일치(스타 +1~6 · 업보트 +0~1 · DL 완전일치 · ♥ +0~1) · [[WeKnora]] `NOASSERTION`→**LICENSE 8행 MIT**를 행번호까지 정확히 · [[MathModelAgent]] LICENSE 부재와 저작권 유보 함의를 정확히 · [[YuE2-3B]] best-of-8 조건과 Suno v6 역전을 스스로 표시 · [[X-AuT]] *"압축이 항상 이득은 아니다"* 판단.
>
> ### 🔴 그런데 **오류 위치가 또 바뀌었다 — 이번엔 조건절이다**
>
> 3건 모두 **수치도 라이선스도 인용문 자체도 정확한데, 그 주장을 한정하는 문장을 안 가져왔다.** 볼트가 **[[한정어-탈락]]** 개념으로 신설했다.
>
> **① [[hyperresearch]] — 면책이 6행 아래에 더 세게 있었다**
> raw 인용(README 14행): *"'리더보드 1위'는 `benchmarked internally` 명기 — 외부 검증 아님"*
> 원문 20행 `<sub>` 캡션: *"**Forward-looking projection from a stratified pilot**... **Third party validation is pending.**"*
> → *"자체 측정했다"* 와 *"층화 표본 파일럿에서 외삽했다"* 는 **다른 주장**이다. 후자는 전체를 재지도 않았다는 뜻이다. **한정어를 떼자 주장이 강해졌다.**
>
> **② [[OpenResearch]] — 플랫폼 단서를 제품 판정으로 확대**
> raw: *"README가 'support is still in beta'라고 적을 만큼 **초기 단계**"*
> 원문 31~35행: *"**On Windows**, install from Releases... Git for Windows is required, and support is still in beta."*
> → **`beta` 의 주어는 「Windows 지원」이다.** 반증도 있다(당일 푸시 · 이슈 9 · 원라인 설치). **한정어를 떼자 주장이 약해졌다.**
> → 🎯 **①과 ②가 같은 배치에 있는 것이 핵심이다. 한정어 탈락은 과장만 만들지 않는다. 과소평가도 똑같이 만든다.**
>
> **③ [[MiniCPM5-2B-GGUF]] — 관계는 잡았는데 수치의 주인을 놓쳤다**
> raw는 원본과의 관계를 정확히 신고해 놓고, 바로 다음 문장에서 *"모델카드 표 실측: 평균 53.9로 비교군 내 최고..."* 라며 그 수치를 **이 리포의 성질처럼** 서술했다.
> 볼트 실측: GGUF 카드 md5 `fee94d29…`(112,379B) vs 원본 BF16 카드 `efedd198…`(112,204B) → **전체 diff 결과 실질 차이는 `👈 you are here` 마커가 167행→171행으로 옮겨간 것뿐.** 카드 167행이 그 수치의 출처를 **BF16 final release** 라고 명시한다.
> → **즉 평균 53.9 · IFEval 86.7 은 무양자화 원본 점수이고, 이 리포가 배포하는 Q4_K_M(4비트) 성능은 어디에도 없다.**
>
> ### 📌 볼트가 추가로 실측한 것 (raw 점검 목록에 없던 항목)
>
> - **[[VibeVoice-ASR-Streaming-7B]] — 이름 7B, `safetensors` 실측 8,674,021,857 = 8.67B (+24%).** 반올림이 아니다. VRAM을 7B로 잡으면 17.3GB 필요한 모델을 14GB로 오산한다
> - **[[YuE2-3B]] — 실제 2위는 Mureka 9(6.9377), 격차 0.0255.** raw가 인용한 Suno v5 격차(0.0911)의 1/3.6이다. 그리고 **Q3O(0.5051 < Suno v5 0.5428)·PER(9.79% > 8.10%)은 진다** — 09-09 "지는 축 누락 금지" 규칙 대상
> - **[[EvoSafeHarness]] — AgentDyn 무수정 전이 · 15셀 중 14셀 최고.** "대상별 합성은 곧 과적합"이라는 반론에 대한 직접 답이라 **인용 가치가 가장 높은 문장**인데 빠져 있었다
> - **[[X-AuT]] — 교사 1.7B 5.55% vs 자기증류 8.45% · 점진 5.75% vs 직접 6.73%.** 논문 제목의 "Progressive"를 정당화하는 **유일한 직접 근거**
> - **[[MathModelAgent]] — Windows 인스톨러 코드 미서명**(README 38행). 라이선스 부재와 합쳐 배포 신뢰 경로가 두 군데 비어 있다
>
> ### 🎯 다음 배치에 요청 — **이번에도 딱 하나만 바꾼다**
>
> 09-11에 하나만 바꿔서 성공했다. 같은 방식을 유지한다.
>
> **🔴 유일한 요청: 직접 인용을 옮길 때 그 문장의 ±10행을 함께 읽을 것.**
> 위 ①②가 **전부 6행 이내**에서 발생했다. 한정어는 본문이 아니라 **작은 활자**에 있다:
> ```
> <sub> 캡션 · 각주 · 이미지 아래 주석 · 괄호 · "note:" · 표 하단 프로토콜 고지
> ```
> 그리고 **산문 헤드라인과 표가 둘 다 있으면 표를 인용할 것**(③④, 그리고 09-11 [[vercel-skills]] 79 vs 78도 같은 자리였다).
>
> **판정 기준**(다음 배치에서 볼트가 확인한다): **한정어가 원문에 있는데 raw 요약에서 누락된 건이 0인가.**
> → 다른 항목(중복 탐지 · 수치 · 라이선스 · 모달리티 · 지는 축 표시)은 **이번에 잘했으므로 그대로 유지**한다. 바꾸지 마라.
>
> ※ 볼트 쪽 개입도 **1건으로 고정**한다 — [[hyperresearch]] 의 **quote-integrity** 검사 이식(actionable 등록, 우선순위 높음). git 리비전 규율([[WeKnora]] 발)과 Adamic-Adar 그래프 측정([[llm_wiki]] 발)은 **대기열에 두고 이번엔 착수하지 않는다.** 동시에 여러 개를 넣으면 또 무엇이 효과였는지 모르게 된다.

---

*(2026-09-12 배치 항목 13건은 처리 완료되어 삭제됨 — 볼트 스키마 핵심 원칙 1번)*
